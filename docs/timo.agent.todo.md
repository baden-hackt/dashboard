# Timo — Agent TODO

> **Spec:** Read `/docs/timo.agent.spec.md` before starting any TODO.
>
> **Rules:**
> - Work ONLY on the TODO marked `ACTIVE`.
> - Do NOT modify LOCKED TODOs.
> - Do NOT create new TODOs.
> - Do NOT change architecture or tooling unless the TODO explicitly requires it.
> - Do NOT refactor unrelated code.
> - Do NOT guess if something is ambiguous — STOP and ask for clarification.
> - When a TODO is complete, change its state from `ACTIVE` to `DONE`.

---

## TODO 1 — DONE
**Goal:** Scaffold Next.js project and configure environment
**Tasks:**
- Run `npx create-next-app@latest dashboard --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*"`
- Create `.env.local` with `NEXT_PUBLIC_API_URL=http://localhost:8000`
- Delete any boilerplate files/content generated by `create-next-app` that are not in the spec (default page content, icons, etc.)
- Create empty directories: `components/`, `lib/`
**Acceptance Criteria:**
- `npm run dev` starts without error on `http://localhost:3000`
- `.env.local` exists with `NEXT_PUBLIC_API_URL`
- `components/` and `lib/` directories exist

---

## TODO 2 — DONE
**Goal:** Implement `lib/api.ts`
**Tasks:**
- Define `FillLevel` interface with all fields from spec: `tag_id`, `fill_level`, `timestamp`, `product_id`, `product_name`, `supplier_name`, `reorder_threshold`, `status`
- Define `Order` interface with all fields from spec: `id`, `tag_id`, `product_id`, `product_name`, `supplier_name`, `supplier_email`, `quantity`, `unit`, `status`, `created_at`, `csv_filename`
- Implement `fetchFillLevels()` — fetches `/api/fill-levels` with `cache: "no-store"`, returns empty array on error
- Implement `fetchOrders()` — fetches `/api/orders` with `cache: "no-store"`, returns empty array on error
- Implement `getCameraFeedUrl()` — returns the camera feed URL string (not a fetch call)
**Acceptance Criteria:**
- File compiles without TypeScript errors
- With Person 2's backend running, `fetchFillLevels()` returns data in the browser console

---

## TODO 3 — DONE
**Goal:** Implement `components/CameraFeed.tsx`
**Tasks:**
- Mark as `"use client"`
- Render an `<img>` tag with `src` set to `getCameraFeedUrl()` + `?t={timestamp}` cache buster
- `useEffect` with `setInterval` updates `timestamp` state every 2 seconds
- `onError` handler dims image to 30% opacity
- `onLoad` handler restores to full opacity
- Style with Tailwind: `w-full rounded-lg border`
**Acceptance Criteria:**
- Component renders without error
- Image refreshes visually every 2 seconds (visible in network tab)
- When backend is down, image dims instead of showing broken image icon

---

## TODO 4 — DONE
**Goal:** Implement `components/ShelfStatus.tsx`
**Tasks:**
- Mark as `"use client"`
- Poll `fetchFillLevels()` every 5 seconds via `useEffect` + `setInterval`
- Render a grid of cards (2 columns mobile, 3 desktop) — one card per product
- Each card shows: product name, product ID, fill level %, fill bar, status label, supplier name
- Color-code cards: green for `ok`, amber for `low`, red for `critical`
- Fill bar uses `transition-all duration-500` for smooth width animation
- Empty state: show "Waiting for data..." when no fill levels available
**Acceptance Criteria:**
- Component renders 2 cards (one per product) when data is available
- Cards are color-coded correctly based on status
- Fill bars animate smoothly when fill levels change
- Shows "Waiting for data..." when backend has no data

---

## TODO 5 — DONE
**Goal:** Implement `components/OrderLog.tsx`
**Tasks:**
- Mark as `"use client"`
- Poll `fetchOrders()` every 10 seconds via `useEffect` + `setInterval`
- Render a table with columns: Time, Product (name + ID), Supplier, Qty, Status
- Format time in Swiss German locale (`de-CH`) showing `HH:MM:SS`
- Status badges: amber `Pending`, green `Delivered`, gray `Cancelled`
- Empty state: show "No orders yet." when no orders exist
**Acceptance Criteria:**
- Component renders a table with correct columns
- Time is formatted as `HH:MM:SS`
- Status badges are color-coded correctly
- Shows "No orders yet." when no orders exist

---

## TODO 6 — ACTIVE
**Goal:** Implement `components/ConnectionStatus.tsx`
**Tasks:**
- Mark as `"use client"`
- Ping `/api/products` every 10 seconds via `useEffect` + `setInterval`
- Show green dot + API URL when connected
- Show red dot + "Backend offline" when disconnected
- Catch all fetch errors — never crash
**Acceptance Criteria:**
- Shows green dot when Person 2's backend is running
- Shows red dot when backend is stopped
- Switches between states within 10 seconds of backend going up/down

---

## TODO 7 — LOCKED
**Goal:** Implement `app/globals.css`, `app/layout.tsx`, and `app/page.tsx`
**Tasks:**
- `globals.css`: only Tailwind directives (`@tailwind base/components/utilities`), no custom CSS
- `layout.tsx`: set metadata title "Lagersystem Dashboard", description "KI-gestütztes Lagerüberwachungssystem", `lang="de"`, dark mode body classes
- `page.tsx`: compose all 4 components in the layout specified:
  - Header row: title left, `ConnectionStatus` right
  - Two-column grid (single column on mobile): `CameraFeed` left, `ShelfStatus` right
  - `OrderLog` full width below
  - Max width `6xl`, centered, with padding
**Acceptance Criteria:**
- `npm run dev` shows the full dashboard with all 4 components
- Layout is two-column on desktop, single-column on mobile
- Page title in browser tab is "Lagersystem Dashboard"

---

## TODO 8 — LOCKED
**Goal:** Verify all polling works with mock data
**Tasks:**
- Start Person 2's backend (or mock it with a simple server returning test JSON)
- Open the dashboard in a browser
- Verify all 4 data sources are polling at correct intervals:
  - Camera feed: network tab shows image request every ~2 seconds
  - Fill levels: network tab shows fetch every ~5 seconds
  - Orders: network tab shows fetch every ~10 seconds
  - Connection status: network tab shows fetch every ~10 seconds
**Acceptance Criteria:**
- All 4 polling intervals are correct (±1 second tolerance)
- Data updates are reflected in the UI without page refresh
- No console errors

---

## TODO 9 — LOCKED
**Goal:** Verify offline/error handling
**Tasks:**
- Stop Person 2's backend while the dashboard is running
- Wait 10+ seconds and observe behavior
- Restart the backend and observe recovery
**Acceptance Criteria:**
- Camera feed dims to 30% opacity when backend is down
- Shelf status shows last known data (does not clear to empty)
- Connection status shows red dot + "Backend offline" within 10 seconds
- After backend restarts, all components recover automatically (green dot, fresh data, camera feed restores)
- Zero console errors or unhandled promise rejections

---

## TODO 10 — LOCKED
**Goal:** Deploy to Vercel
**Tasks:**
- Push the `dashboard/` repo to GitHub
- Connect the repo to Vercel (or run `npx vercel`)
- Set `NEXT_PUBLIC_API_URL` environment variable in Vercel dashboard to the laptop's local IP (e.g. `http://192.168.1.42:8000`)
- Trigger a deploy
**Acceptance Criteria:**
- Vercel deployment succeeds
- Dashboard loads at the Vercel URL
- If on the same network as the laptop, data loads from Person 2's API
- If NOT on the same network, dashboard shows "Backend offline" (expected — the fallback is `npm run dev` locally)

---

## TODO 11 — LOCKED
**Goal:** Physical demo setup
**Tasks:**
- Print AprilTags: download tag36h11 tag IDs 0 and 1 from https://github.com/AprilRobotics/apriltag-imgs/tree/master/tag36h11, print at minimum 6cm × 6cm on white paper
- Set up a small shelf or rack with 2 visible compartments
- Tape one AprilTag to the bottom-front edge of each compartment (ID 0 on left, ID 1 on right)
- Fill each compartment with items (boxes, bottles, whatever is available)
- Position the webcam on a tripod aimed directly at the shelf from the front
- Verify each AprilTag is at least 80×80 pixels in the camera frame
**Acceptance Criteria:**
- 2 AprilTags printed and taped to shelf
- 2 compartments have items in them
- Camera has clear line of sight to both tags
- Person 1's pipeline detects both tags when pointed at the shelf

---

## TODO 12 — LOCKED
**Goal:** End-to-end integration test
**Tasks:**
- All 3 systems running: Person 1's pipeline, Person 2's backend, Person 3's dashboard
- Remove an item from one shelf compartment
- Wait 5–10 seconds
- Observe the full flow on the dashboard
**Acceptance Criteria:**
- Camera feed shows updated shelf (item removed)
- Fill level drops on the corresponding product card (color changes from green to amber/red)
- Order appears in the order log table with status "Pending"
- Email is sent to the supplier (check inbox)
- Removing more items from the same product does NOT trigger a duplicate order

---

## TODO 13 — LOCKED
**Goal:** Final handoff
**Tasks:**
- Verify dashboard loads correctly on Vercel (or locally via `npm run dev`)
- Verify all 4 components render and poll at correct intervals
- Verify physical shelf setup is stable (tags don't fall off, camera doesn't move)
- Verify fallback plan works: if Vercel can't reach the laptop, `npm run dev` on the laptop works
- Clean up any console warnings or TypeScript errors
**Acceptance Criteria:**
- Dashboard is live and showing real data from the shelf
- Physical demo setup is stable and ready for judges
- Fallback plan tested and confirmed working
